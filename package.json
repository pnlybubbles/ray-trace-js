{
  "name": "ray-trace",
  "version": "1.0.0",
  "description": "ray-trace on js",
  "main": "index.js",
  "scripts": {
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify -v -d index.babel.js -t [ babelify --presets [ es2015 ] ] -o 'exorcist index.js.map > index.js'"
  },
  "author": "pnlybubbles",
  "license": "MIT",
  "dependencies": {
    "gl-matrix": "^2.3.2",
    "superagent": "^1.8.3"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.3.0",
    "exorcist": "^0.4.0",
    "http-server": "^0.9.0",
    "watchify": "^3.7.0"
  }
}
